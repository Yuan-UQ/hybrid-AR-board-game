"""
Event point domain model.

Event points are temporary digital entities rendered on the UI (no physical markers).
They have:
- type (ammunition/medical/trap)
- spawn position
- lifecycle metadata (spawn round, triggered/valid flags)
"""

